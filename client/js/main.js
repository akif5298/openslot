/**
 * OpenSlot – client entry point
 * Booking and managing professor office hour appointments
 */

document.addEventListener('DOMContentLoaded', () => {
  const main = document.querySelector('.app-main');
  if (main) {
    main.innerHTML = '<p class="placeholder">Welcome to OpenSlot. Connect the API and add booking UI here.</p>';
  }
});
